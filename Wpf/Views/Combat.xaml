<reactiveUi:ReactiveUserControl
    x:TypeArguments="viewModels:MissionStatsViewModel"
    x:Class="Wpf.Views.Combat"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:reactiveUi="http://reactiveui.net"
    xmlns:viewModels="clr-namespace:Wpf.ViewModels"
    xmlns:controls="clr-namespace:Wpf.Views.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <ListView x:Name="FactionList" Grid.ColumnSpan="3">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Faction" DisplayMemberBinding="{Binding Path=Name}" />
                    <GridViewColumn Header="Mission 1"  DisplayMemberBinding="{Binding Path=Mission1Remaining}" />
                    <GridViewColumn Header="Mission 2"  DisplayMemberBinding="{Binding Path=Mission2Remaining}"/>
                    <GridViewColumn Header="Mission 3"  DisplayMemberBinding="{Binding Path=Mission3Remaining}"/>
                    <GridViewColumn Header="Mission 4"  DisplayMemberBinding="{Binding Path=Mission4Remaining}"/>
                    <GridViewColumn Header="Kill Total"  DisplayMemberBinding="{Binding Path=KillsRemaining}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Row="1">
            <controls:DataWithLabel x:Name="StackHeight" Label="StackHeight: " />
            <controls:DataWithLabel x:Name="StackWidth" Label="Stack Width: " />
            <controls:DataWithLabel x:Name="MissionCount" Label="Mission Count: " />
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1">
            <controls:DataWithLabel x:Name="TotalKills" Label="Total Kills: " />
            <controls:DataWithLabel x:Name="Ratio" Label="Ratio: " />
            <controls:DataWithLabel x:Name="TotalPayout" Label="Total Payout: " />
            <controls:DataWithLabel x:Name="MissionAverage" Label="Mission Average: " />
            <controls:DataWithLabel x:Name="MillPerKill" Label="Reward Per Kill: " />
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="2">
            
        </StackPanel>
    </Grid>
</reactiveUi:ReactiveUserControl>